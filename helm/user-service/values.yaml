replicaCount: 1
fullnameOverride: "user-service"

image:
  repository: xlen911/user-service
  pullPolicy: Always
  tag: latest

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi